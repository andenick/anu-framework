# Changelog

The Anu Framework changelog lives at
[`docs/ANU_FRAMEWORK_CHANGELOG.md`](docs/ANU_FRAMEWORK_CHANGELOG.md).

This file used to be a byte-identical copy of it, published at a path where its
relative links could not resolve. It is now a pointer so there is exactly one
changelog to keep true.
