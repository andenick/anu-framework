"""P02_{{sid}} — Process {{name}}; pass-through processor.

The book-period values ARE the canonical published values; no transformation
is applied. Extension/splice stages (if applicable) add rows in later
pipeline runs.

Generated by anu-scaffold v1.0.
"""
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from L01_loaders.L01_{{sid}}_{{slug}} import LOADER  # noqa: E402
from utils.series import run_pipeline_for_series      # noqa: E402  from anu-replicator lib/ template


def run():
    final_path = run_pipeline_for_series(LOADER, "{{source_file}}:{{source_column}}")
    print(f"    [P02_{{sid}}] wrote {final_path.name}")


if __name__ == "__main__":
    run()
